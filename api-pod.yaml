apiVersion: v1
kind: Pod
metadata:
  name: farmacia.api
  labels:
    app: api-pod
spec:
  containers:
  - name: farmacia-container
    image: duartefull/farmacia:1.2
    envFrom:
    - configMapRef:
        name: db-configmap
    ports:
    - containerPort: 8080